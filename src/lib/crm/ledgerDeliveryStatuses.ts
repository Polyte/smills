/** Suggested delivery statuses for the sales ledger (custom values still allowed). */
export const LEDGER_DELIVERY_STATUS_PRESETS = [
  "Partly Delivered",
  "Not Delivered",
  "Delivered & Signed",
] as const;
